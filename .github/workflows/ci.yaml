name: Lab 59 CI

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello from GitHub Actions"

      - name: Show current directory
        run: pwd

      - name: List files
        run: ls -la

      - name: Lab 59 marker
        run: echo "This run was triggered by my latest push"
