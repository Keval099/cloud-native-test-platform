# Futuristic GitHub Pages portfolio

Updated `portfolio/index.html` changes:

- Header `GH` → `Github`
- Header `in` → `LinkedIn`
- Removed the theme control
- Tech stack now uses:
  - Devicon for AWS, Kubernetes, Terraform, Docker, Python, GitHub Actions, Linux and Bash
  - AWS Architecture Icons for Amazon CloudWatch, Amazon ECR and Amazon VPC
  - Simple Icons for Trivy
- Screenshot references remain relative to `assets/` and use lazy loading.

The AWS service icon URLs use an AWS Architecture Icons mirror based on the official AWS icon set; the icon repository documents these as official AWS architecture icons. The Simple Icons CDN documents its URL format for Trivy.

Replace `YOUR_LINKEDIN_ID` with your actual LinkedIn public profile path.
